# Password Generator

This is a simple command-line password generator built using Python.

It allows the user to:
- choose the length of the password
- decide whether to include numbers
- decide whether to include symbols

Based on the input, the program generates a random password.

## How to run

1. Make sure Python is installed  
2. Run the file:

```bash
python main.py
